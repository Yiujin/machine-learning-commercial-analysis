두번째꺼 y2 매출액으로 하면 x 

인구, 소득분위. 유사업종을 (년월 , 상권, 업종별)로 나누기 -> 매출액 

년월을 어떻게 넣어줄건지. 년도 -> 계절, 그때 무슨일이 있었는지
예측할 때 어느동네에서 어떤업종을 하는데 그때당시에 인구와 업종과 그런걸 넣으면
매출액을 알려줄수 있는데 거기에 계절까지 들어가면 더 좋음 분기로 넣어도됨

학습데이터가 18년 9월 10월까지 몇 개 업종, 상권, 총 몇건인지 조사해야됨
업종 : 43개
상권 : 1744개

상권을 줄이고 계절을 1년치 가져올수 없니? ㄴㄴ 데이터마다 달라서 안됨
그러면 상권은 있고 년월은 없다? -> 그러면 년월이 가장길면서 공백이 가장없는
상권을 정해라 ->상권이 소뷴류되는 규칙잇냐? -> 전체상권 할필요없이 특정상권만 줘라

모형에서는 전체를 주는것보다 더 작은 동질집단 대상으로 하는것이 정확도가 더 높아진다

학습데이터 정해줘야됨 
절차 : 분석목적정의 -> 데이터 이해 -> 변수 선정 -> 알고리즘 설계-> 학습 -> 평가

분석목적정의 : 모형은 회귀/분류/  등 문제유형, y,x 변수 값 나옴 -> 이걸가지고 모형정의서라고 하는 산출물 

데이터이해 : 년월은 뭐고 상권은 뭐고 뭐다. 그러면 년월을 6자리 문자로? 월이라고하는 숫자로? 반기라고하는 1,2,3,4의 (생긴건 숫자지만)명목형으로?
		-> 1부터 12까지 sum으로 나누어보고 의미가 있으면 월로 주자
		-> 분기별로 나누어서 sum 으로 비교,,,, 해서 결정

변수 선정 : 데이터 이해를 거쳐 변수의 형태(숫자, 이진, 명목(구간))를 결정, 단위(금액그대로, z스코어, 표준화)를 결정(ex-월을 숫자로? 더미로 원핫인코딩?)
		->변수정의서가 나옴 x1~xn까지 뭐는 정수형 뭐는 명목형이다
		-> feature selecting 
알고리즘설계 : 회귀 예측모형이면 회기, 신경망 등 알고리즘 결정
		->학습데이터와 테스트데이터 정의해야됨 얘네들의 기간, 건수, 주요 업종상권에 대한 카테고리 갯수가 나와야됨 
평가 : 정확도, 오류 계산  
 
// /////
모형정의서(한글에 있는거), eda 결과서(로그의 형태) ->  변수선별을 위해서지만 연령대별로 유의라더라 업종코드가 몇번이며 얘네들은 20대 매출액이 비어있어야될것같다 등의
가설을 세우고 검증할수 있음  반복하면서 변수 정의에 영향, 학습데이터, 알고리즘 설계서, 평가 결과

나중에 가면 상권 정보가 있으면 상권 인구, 매출 등 여기서 필요한 데이터들을 조인해서 하나의 입력 데이터 셋을 만들고 그 입력데이터셋의 키를 정해야됨 
group by sum : 우리가 예측하고자하는 단위가 년별, 상권별,연령대 별이다 그러면 얘네를 키로 가져가서 group  by sum 

기업 부도 예측 데이터 KIS VALUE -> 구해서 대신 재무재표 정보들어가있으니까 한.달.이.면.끝.나^^ 끝나면 이걸로 해보는것도 재미있겠당ㅎㅎ

다음달에 만날때까지 기본모형 만들어와 
모형정의서, 변수 정의서와 학습데이터 정의서(학습과 시험데이터에 대한 기간, 카테고리(상권이 몇개냐 성별이 몇개야), 데이터건수) , 학습데이터 세트까지 만들어와 
다하면 모형설계서(회귀(선형, 피널라이즈드(벌점형-베타계수가 커지는거에대해 벌점을 줌), 라쏘(베타를 0으로)), k-means) 가져와 
